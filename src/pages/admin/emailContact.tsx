import EmailContactList from '@/components/Admin/CustomerService/EmailContactList';
import React from 'react';

const EmailContact = () => {
    return (
        <div>
            <EmailContactList />
        </div>
    );
};

export default EmailContact;
